source
| where AppRoleName <> "altinn-prod-api Norway East"
| where Name != "POST /graphql/{**slug}"
| where Name != "OPTIONS /graphql/{**slug}"
| where Name !startswith "GQL: subscription"
| where Name !startswith "GET /health"
